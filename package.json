{
  "name": "learning_astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler types && astro dev",
    "start": "wrangler types && astro dev",
    "build": "wrangler types && astro check && astro build",
    "preview": "wrangler types && wrangler pages dev ./dist --port=4321",
    "deploy": "wrangler types && astro check && astro build && wrangler pages deploy",
    "astro:dev": "astro dev",
    "astro:build": "astro build",
    "astro:preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/cloudflare": "^12.4.0",
    "@tailwindcss/vite": "^4.1.3",
    "astro": "^5.6.1",
    "tailwindcss": "^4.1.3",
    "typescript": "^5.8.3",
    "wrangler": "^4.7.2"
  }
}
